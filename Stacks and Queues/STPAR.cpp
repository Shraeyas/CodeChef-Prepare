#include <iostream>
#include <vector>
#include <stack>

using namespace std;

int main()
{
  freopen("in.in", "r", stdin);

  int n;

  while(cin >> n, n)
  {
    vector <int> v(n);
    for(int i = 0 ; i < n ; i++)
    {
      cin >> v[i];
    }

    stack <int> stk;
    vector <int> ans;


  }
}
